\begin{figure*}[t]

  \begin{panel}{(a)}{\textwidth}
    \vspace{5mm}
    \small
    \input{figures/panels/overview.tikz.tex}
    \vspace{-2mm}
  \end{panel}

  \begin{panelcolumn}{0.55\textwidth}
    \begin{panel}{(b)}{\textwidth}
      \small
      \centerline{\input{figures/panels/transition_model.tikz}}
    \end{panel}

    \begin{panel}{(c)}{\textwidth}
      \small
      \hspace{4mm}
      \input{figures/panels/intensity_model.tikz}
    \end{panel}
  \end{panelcolumn}
  \begin{panel}{(d)}{0.4\textwidth}
    \small
    \vspace{4mm}
    \hspace{4mm}
    \centerline{\input{figures/panels/trace_model.tikz}}
  \end{panel}

  \caption{
      \panelref{a} Overview of the \ours method (scale bar: 1 $\mu$m). 
      \ours fits each detected spot individually, extracting a time series 
      of intensity measurements, fitting our model and producing a posterior 
      over possible counts. It is assumed that each spot is made up of $n$ independent 
      emitters.
      %
      \panelref{b} \pon and \poff (\parameterst) parameterize the blinking kinetics 
      of each individual emitter
      %
      \panelref{c} The distribution of observed intensities is affected 
      by both Poisson distributed shot noise (\parameterse), and 
        Gaussian distributed readout noise (\parametersc).
      %
      \panelref{d} \ours is based on a hidden Markov model conditioned on the 
      total count $n$ and seven estimated parameters.
  }
  \label{fig:method:overview}
\end{figure*}
