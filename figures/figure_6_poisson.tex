\begin{figure}[t]
  \begin{panel}{}{\textwidth}
    \small
    \centerline{\input{figures/panels/poisson_normal.tikz.tex}}
  \end{panel}
  \caption{%
    KL divergence between a Poisson distribution ($\poisson[\lambda]$) and a
    normal distribution ($\mathcal{N}[\lambda,\lambda]$) as a function of the
    average photon count per frame $\lambda = (\z{}\re + \rb)\deltat$. The
    insets show a direct comparison of the normal distribution (green) vs.\ the
    Poisson distribution (orange) for a low photon count of $\lambda=81$
    corresponding to a KL divergence of $10^{-3}$ (left inset) and the
    experimentally observed $\lambda=2048$ (right inset; with $\z{}=1$,
    $\re=2.99$, $\rb=7.43$, and $\deltat=200ms$).
  }
  \label{fig:poisson_vs_normal}
\end{figure}
